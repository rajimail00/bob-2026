# The Express API base. Do not add a trailing slash.
BACKEND_API_URL=https://bobbk-api.cifarsystems.com/api/v1

# Local: http://localhost:3001. Production: the final HTTPS admin origin.
ADMIN_APP_URL=http://localhost:3001

# Generate a unique value of at least 32 random characters for every environment.
ADMIN_SESSION_SECRET=replace-with-a-long-random-secret
